Check that tests are meaningful, not just "compile and pass."

- Tests cover same scenarios as source tests
- No stubbed/skipped/empty bodies
- Assertions check behavior (not just error != nil)
- Missing negative tests = flag
- Test helpers reused, not duplicated
