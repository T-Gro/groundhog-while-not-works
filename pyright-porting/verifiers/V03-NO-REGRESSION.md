EXECUTABLE GATE — run by orchestrator, not by LLM.
Checks: passing_count >= previous AND total_count >= previous.
Hard gate: regressions block the sprint.
On failure: list of regressed tests fed back to implementor.
