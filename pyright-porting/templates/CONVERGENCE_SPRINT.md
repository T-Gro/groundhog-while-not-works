---
type: convergence-sprint
layer: {layer_id}
target_feature: {feature_name}
previous_passing_pct: {N.N}
target_delta_pct: {+N.N}
---
# Convergence Sprint: {title}

## Goal
Increase oracle test passing from {previous}% to ~{target}% by porting {feature}.

## Context
- **Layer**: {layer_name} ({layer_description})
- **Source files**: {list of source files in scope}
- **Estimated tokens**: ~{N}K source
- **Depends on**: {what must already be ported}

## Current Failure Analysis
{Compact summary of WHY tests are failing, generated by FailureAnalyzer}

### Representative Failures (top 20)
```
{failures inserted by ContextBuilder}
```

## Adjacent Layer Interfaces
{Compact interface contracts from hints/layer-boundaries.md — ONLY adjacent layers}

## Implementation Steps
1. Create/update target-language package at `{package_path}`
2. Define types matching the source language structures
3. Implement each exported function
4. Write tests matching each source test scenario
5. Run build command and fix any errors
6. Run test command and fix any failures
7. Run oracle comparison and verify Δ% improvement

## What to Avoid
- Do NOT use untyped escape hatches unless the source is truly untyped
- Do NOT stub functions — every function must be fully implemented
- Do NOT modify code outside this sprint's package scope
- Do NOT reduce the number of passing oracle tests (regression = hard fail)

## Definition of Done
- [ ] Build command succeeds
- [ ] Lint/vet command clean
- [ ] Tests pass for this package
- [ ] Oracle passing % ≥ previous sprint's %
- [ ] No TODO/FIXME placeholders remain
- [ ] Every exported source function has a target counterpart
